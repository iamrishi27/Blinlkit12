import React from "react";

const Footer = () => {
  return (
    <footer className="app-footer">
      <small>© {new Date().getFullYear()} IntellMeet</small>
    </footer>
  );
};

export default Footer;
